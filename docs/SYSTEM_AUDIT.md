# Аудит системы — 2026-08-05

Аудит выполнен безопасными локальными командами без полного сканирования диска и без изменения внешних программ.

| Компонент | Результат |
|---|---|
| ОС | Windows 10, build 19045, x64 |
| PowerShell | 5.1.19041.3803 Desktop |
| CPU | Intel Core i7-9700K по данным пользователя, 8 логических потоков подтверждены Python |
| RAM | 49 083 MiB всего, обычно около 39 000 MiB доступно во время аудита |
| GPU | NVIDIA GeForce RTX 2070 SUPER, 8 192 MiB, Turing |
| Драйвер NVIDIA | 610.74 |
| VRAM | менялась от ~1.05 до ~7.05 GiB свободной из-за внешней нагрузки |
| GPU-процессы | 19 графических/вычислительных клиентов; проект их не завершал |
| CUDA toolkit | `nvcc` не найден; для PyTorch wheel он не требуется |
| PyTorch | финально `2.11.0+cu126`, CUDA 12.6 доступна, GPU распознан |
| Python launcher | `C:\Windows\py.exe` есть, но `py -0p` не показывает установленных Python |
| Проектный Python | CPython 3.12.13 в `.venv` |
| Git | 2.49.0.windows.1 |
| FFmpeg | 8.1.2, кодирование WAV/MP3 проверено |
| SoX | не найден; текущий путь использует soundfile/FFmpeg и работает без него |
| Диск D: | около 1.77 TiB свободно перед скачиванием модели |
| Backend | порты 8020/8021 проверены локальными серверами |
| ComfyUI | не найдена в безопасно проверенных стандартных местах |
| SillyTavern | не найдена в безопасно проверенных стандартных местах |

Получение части CIM-данных было запрещено средой, поэтому модель CPU взята из постановки пользователя; логическое число потоков и рабочие ресурсы проверены через Python/psutil. Shared GPU memory достоверно получить без расширенного системного доступа не удалось; вместо этого auto-режим использует dedicated free VRAM и наличие GPU-процессов, не вмешиваясь в них.

Во время CUDA-теста модель увеличила использование VRAM примерно с 0.95 до 3.59 GiB; после остановки только проектного процесса свободная VRAM вернулась примерно к 7.05 GiB.

## Дополнение 2026-08-06: ComfyUI

Официальная ComfyUI Windows Portable NVIDIA `0.30.0` установлена отдельно в `D:\Folder\ia\ComfyUI_windows_portable`. Archive прошёл SHA256 и `7z t`; чистый запуск до custom nodes ответил на `/system_stats` только через `127.0.0.1:8188`. Embedded stack: Python 3.13.14, PyTorch 2.13.0+cu130; GPU распознан как RTX 2070 Super. Manager установлен из shipped `manager_requirements.txt` (`comfyui-manager 4.2.2`), `pip check` прошёл.

Семь Qwen TTS нод зарегистрированы в категории `Qwen TTS API`; единственная строка их импорта занимает 0.0 с, `IMPORT FAILED`, `ModuleNotFoundError` и traceback отсутствуют. `qwen_tts` в embedded Python не установлен. Ноды подключены безопасным копированием; junction был отклонён средой. Старые копии хранятся вне сканируемой папки в `ComfyUI\.qwen_tts_api_nodes-backups`.

Измерения одного реального workflow: до сервисов RAM free 34 034 MiB / VRAM used 1 180 MiB; после backend+ComfyUI до синтеза 32 834 MiB / 1 281 MiB; во время Qwen on-demand до 6 268 MiB VRAM; после job 32 907 MiB RAM free / 1 289 MiB VRAM used. Внешние процессы не завершались и не изменялись.
